#include <stdlib.h>
#include <stdio.h>
#include "lists.h"

/**
 * find_listint_loop - define function
 * @head: only declare a maximum of two variables in your function
 * Return: what does it return?
 */
listint_t *find_listint_loop(listint_t *head);
{
	head++;
	return (0);
}
